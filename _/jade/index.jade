include _config.jade
include _mixins.jade

doctype html
html(lang="es")
	head
		+head(titulo + " | " , stylesheets, keywords, description)
	body.home
		.wrapper
			+header('index', logoAlt)
			.impulsamos
				img(src="_/img/impulsamos-ventas.png", alt="Impulsamos las ventas dentro de la tienda")


		+scripts